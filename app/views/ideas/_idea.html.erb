<div id="<%= idea.id %>">
  <h3><%= idea.title %></h3>
  <p><%= idea.body %></p>
  <p>Quality: <%= idea.quality %></p>
  <%= button_to "Delete", idea_path(idea.id), class: :remove, remote: true, method: :delete %>
</div>
